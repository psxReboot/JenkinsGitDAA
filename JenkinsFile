pipeline{
	   agent any
	            stages {
			    stage('One') {
                                        steps {
					                echo 'Hi, this is Dyubkin2'
					}
	 		  }
			    stage('Two') {
                                        steps {
					               script {
                 					       env.RELEASE_SCOPE = input message: 'Нужен ввод пользователя', ok: 'Release!',
                                                               parameters: [string(name: 'RELEASE_SCOPE', defaultValue: '', description: 'What is the release scope?')]
                                                              }
                                               echo "${env.RELEASE_SCOPE}"
					}
	 		  }
	  
	            }
}